<div class="container">
  <%= form_for section do |form| %>

  <% if section.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(section.errors.count, "error") %> prohibited this section from being saved:</h2>

    <ul>
      <% section.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
  <% end %>

  <div class="field">
    <%= form.label :section %>
    <!-- <td class="col-md-4"><%= form.select(:section, ['Kindergarten Section', 'Junior Section(Class 1 to 4)','Junior Section(Class 1 to 5)',"Boy's Section(class 5 to 12)","Boy's Section(class 6 to 12)", "Girl's Section(class 5 to 12)","Girl's Section(class 6 to 12)"]) %></td> -->
    <!-- <td class="col-md-4"><%= form.select(:section, ['Kindergarten Section', 'Junior Section(Class 1 to 4)','Junior Section(Class 1 to 5)',"Boy's Section", "Girl's Section"]) %></td> -->

    <%= form.text_field :section, class: 'form-control' , required: 'required' %>
  </div>

  <div class="actions mt-4 text-center">
    <%= form.submit %>
  </div>
  <% end %>
</div>
