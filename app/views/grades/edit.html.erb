<div class="container section">
  <h1>Editing Grade</h1>

  <%= render 'form', grade: @grade %>

  <%= link_to 'Show', @grade %> |
  <%= link_to 'Back', grades_path %>
</div>

<%= render 'welcome/footer' %>
